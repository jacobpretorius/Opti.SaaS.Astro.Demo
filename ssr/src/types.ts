export interface SiteMeta {
  name: string;
  description: string;
  logo?: string;
  twitterHandle?: string;
  // Add other site-wide metadata properties as needed
}

// You can define other shared TypeScript interfaces or types here.
// For example, if you have a common structure for frontmatter in Markdown files:
/*
export interface MarkdownFrontmatter {
  title: string;
  description: string;
  pubDate: string;
  updatedDate?: string;
  heroImage?: string;
  tags?: string[];
}
*/ 