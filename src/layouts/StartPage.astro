---
const { pageContent } = Astro.props;
---

<h1>🧑‍🚀 {pageContent.Heading}</h1>
<div set:html={pageContent.Body} />
<div class='hero-container'>
  {
    pageContent.BannerImages.map((item, i) => (
      <img src={item.ContentLink.Expanded.Url} key={i} />
    ))
  }
</div>

<style>
  .hero-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    overflow: hidden;
  }
  .hero-container img {
    border-radius: 10px;
    width: calc((100% - (4 * 1em)) / 4);
    margin: 1em;
    flex: 1;
    object-fit: cover;
  }
</style>
